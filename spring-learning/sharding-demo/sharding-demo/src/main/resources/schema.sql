create table t_user (
                        id bigint not null auto_increment,
                        name varchar(255),
                        age integer,
                        primary key(id)
);
